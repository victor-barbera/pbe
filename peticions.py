import requests

def
if __name__ == __main__ :
    args = { 'nom1' : 'guifre', 'nom2' : 'victor' }
    res = requests.get("https://postman-echo.com/get",params=args)

    print(res.json()['args']['nom1']))
        
    def createTable(self, tableName, rows) :
        if(tableName == "timetables") :
            columns = ["day", "hour", "subject", "room"]
            listmodel = Gtk.ListStore(str, str, str, str)
        if(tableName == "tasks"):
            columns = ["date", "subject", "name"]
            listmodel = Gtk.ListStore(str, str, str)
        if(tableName == "marks":            columns=["subject","name","mark"]        listmoel = Gtk.ListStore(str, str, str)
#             {"tableName" : "timtables",
#              "rows" : [
#                 ["icom","guire","4"],
#                 ["dsbm", "victor", "8"]
#            ]}
            
        
    
    def createTable(self,es.jso()["ableName"],["rows"]